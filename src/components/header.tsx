import React from 'react';
import logo from '../assets/svgs/logo.svg';

const Header = (): JSX.Element => {
  return <header>{/* <img src={logo} alt="em.wedding logo" /> */}</header>;
};

export default Header;
